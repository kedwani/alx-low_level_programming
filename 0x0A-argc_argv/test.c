#include <stdio.h>
#include <stdlib.h>
   /* void main()
{
char str[]="0";
    int a = atoi(str);
    printf("%d",a);
}
    */

int main( int *argv[])
{
        printf("%ls\n", argv[2]);
        return (0);
}
