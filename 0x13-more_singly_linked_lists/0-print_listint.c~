#include "lists.h"
/**
 *print_listint - is a function to print content of linked list.
 *@listint_t : is the header node.
 *Return: the number of printed nodes.
*/

size_t print_listint(const listint_t *h)
{
	unsigned int i = 0;
	while(h)
	{
		printf("%d\n",h->n);
		h = h->next;
		++i;
	}
	return(i);
}
